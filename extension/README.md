# Casting Next 収集ツール（Chrome拡張）

instagram.com のログインセッションで、キャスティング管制室の収集を**ボタン1回**で行う拡張です。
**Claude（LLM）を経由しません。** 書き込み操作は**候補ボードでチェックした候補へのDM送付だけ**です。
フォロー・いいね・投稿・UA偽装は一切しません（設計書_DM自動一括送付 §0）。

収集ロジックは本体と同一（`kit/ig_probe.js` と `kit/prof_compact.js` を `igf/` に同梱）。
この拡張は「instagram.com のタブで収集を回して jsonl を保存する」配線だけを担います。

## なぜ拡張なのか

ダッシュボード（GitHub Pages）は `github.io`、収集対象は `instagram.com` の別オリジンです。
Webページからは別オリジンのログインAPIを叩けません。instagram.com のタブの中でコードを
動かす「乗り物」が必要で、それが拡張です（旧フローで Claude拡張がやっていた役割そのもの・AI抜き）。

## インストール（初回だけ）

1. Chrome で `chrome://extensions` を開く
2. 右上の「デベロッパーモード」をオンにする
3. 「パッケージ化されていない拡張機能を読み込む」→ この `extension/` フォルダを選ぶ
4. instagram.com に、収集に使うアカウントでログインしておく

## 使い方

1. ダッシュボード（casting-next）で「プールから取得」→「キューを作る」を押す
   （キューが localStorage に書き出されます）
2. ツールバーの拡張アイコンをクリック → ポップアップで「収集を実行」
   - ダッシュボードのタブが開いていればキューを自動取得します
   - 取れない場合は、ハンドルを1行1件でポップアップに貼り付けてもOK
3. instagram.com のタブで収集が回り（1件4〜12秒間隔）、終わると
   `Downloads/casting-next/<run>_compact.jsonl` が保存されます
4. その jsonl をダッシュボードの「取得結果のドロップ」へ入れる → 採点・分析（ここは従来どおり）

進捗はポップアップに出ます。**ポップアップを閉じても収集とダウンロードは完走します**
（収集は instagram.com のタブ側で走るため）。`rate_limited` を検出したら安全に停止します。
時間を置いて同じ手順で再実行すれば、ダッシュボードの取得済み除外で続きから取れます。

## やらないこと

フォロー・いいね・投稿・UA偽装は一切しません。**チェックされていない候補にDMを送りません**
（巡回・全件・自動継続送信をしません）。解除したのは「人が選んだ相手へのDM送付」だけです。

## うまく動かないとき

- ポップアップに「ログインしていません」と出る → instagram.com のタブでログインし、
  そのタブを一度再読込してから再実行
- 「タブへの送信に失敗」→ instagram.com のタブを再読込（拡張の content script を読み直す）
- キューが取れない → ダッシュボードで「キューを作る」を押したか確認、またはハンドルを手貼り
- 取得は成功するのに未ログイン扱いになる（分離ワールドのfetchでCookieが乗らない場合）→
  既知の代替として MAIN ワールド注入に切り替える改修が必要。発生したら報告してください

## ファイル

- `manifest.json` — MV3設定
- `content_ig.js` — instagram.com タブで動く収集ランナー（IGF+__PROF を使う配線）
- `background.js` — 最終JSONLをダウンロード
- `popup.html` / `popup.js` — 操作UI（キュー取得・実行・進捗）
- `igf/ig_probe.js` `igf/prof_compact.js` — 収集ロジック（`../kit/` と同一。更新時は再コピー）

## ① 発掘して収集（v1.1・2026-08-03）

ポップアップの「①発掘して収集」タブで、タグから候補アカウントを見つけて、そのまま取得まで
ボタン1回で行います（Claude非経由）。

- 手順: タグを1行1件で入力（先頭に `*` を付けると生活文脈タグ扱い）→ 目標件数 → 「発掘して収集」
- 「ダッシュボードのタグを取り込む」を押すと、casting-next の「①発掘から始める」で選んだタグ・
  目標件数・取得済みリストをそのまま取り込めます（この連携は collect.js の変更が公開版に反映されて
  いることが前提。手入力なら反映前でも動きます）
- 中身: 各タグで `IGF.hashtag` と `IGF.search` を叩き、投稿者ハンドルといいね数を集めて、
  discovery_template.md のルール（タグ横断→生活文脈→いいね100〜8,000→いいね不明→法人語/媒体名は
  最後尾）で並べ替え、取得済みを除いて上位を取得します。並べ替えは best-effort（法人/人名判定は近似）
- タグ応答の形は実データ(2026-08)で確認済み。出力は `disc_compact.jsonl`

## 自動反映と実行の注意（v1.2・2026-08-03）

- 収集/発掘が終わると、`<run>_compact.jsonl` を保存すると同時に、**ダッシュボードのタブが開いていれば
  自動でそこに流し込んで解析まで走らせます**（ドラッグ&ドロップと同じ経路を再現。ダッシュボードのコードは変更なし）。
  収集画面でなくても、background が `#/collect` に移動してから取り込みます。開いていなければ保存した
  jsonl を手動でドロップしてください。
- **instagram.com のタブは開いたままにしてください。** 収集はそのタブの中で走ります。ポップアップは
  閉じてOK、別タブで作業してもOK。ただしそのIGタブを閉じる/別サイトに移動すると中止します。

## ③ 精査データ収集（v1.4・2026-08-03）— これで収集は全工程Claude非経由

ポップアップ「③ 精査データ」タブで、精査待ちの上位アカウントの
**キャプション全文・コメント欄・bio全文**（3ファイル/名）を集めます。最終判断は人が行います（設計どおり）。

- 手順: 「ダッシュボードの精査待ちを取り込む」→ 人数（既定2名）→「精査データを集める」
- 出力: `<handle>_captions.txt` `<handle>_comments.txt` `<handle>_profile.txt` を
  `Downloads/casting-next/qual/` に保存。**先頭1名は精査画面へ自動反映**（残りは順にドロップ）
- captions/profile（bio含む）は IGF.profile の全文から生成（実データで書式検証済み）
- comments は `/api/v1/media/<id>/comments/` を叩いて形無依存で抽出。取得できない投稿は
  「取得できませんでした」と明記（推測で埋めない）。media_id が取れない口（v1_feedのみ等）では
  コメントが空になることがある（captions/profile は影響なし）

## ④ DM送付（v1.1・2026-08-04）— 唯一の書き込み経路

正は `docs/設計書_DM自動一括送付_v1.0.md`。**送ったDMは取り消せません。** 手順を飛ばさないでください。

1. ダッシュボードの**候補ボード**で、送りたい候補にチェックを入れる（先頭の選択列）
2. 「選択N件にDMを送る」→ 一括DMパネル。**全文を読んで**、必要なら textarea で直す
   - 既にDM送付済み／適合コメント未記入／見送り／紹介者／NG常習／第0候補(10万超)は
     **チェックしていても送付対象から外れます**。理由はパネルに出ます（黙って落としません）
3. 「送付キューを書き出す」→ 拡張の「④ DM」タブ →「送付キューを取り込む」→「送付を開始」
   - 拡張は user_id を解決して**下書きを作るところまで**。送信はしません
   - 監査ログが `Downloads/casting-next/dm/<日付>_dm_log.jsonl` に出ます
4. popup の下書き一覧で「開く」→ DM画面に本文が入るので、**ご自身で送信ボタンを押す**

### モード

| モード | 挙動 |
|---|---|
| **半自動（唯一のモード）** | 拡張は下書きを作るところまで。popup の下書き一覧から「開く」でDM画面が開き、本文が入ります。**送信ボタンは人が押します** |
| ~~全自動~~ | **使えません。** 設計書§5-3 の送信APIが現行のInstagram webに存在しないことを実機で確認（下記） |

### なぜ全自動が無いのか（2026-08-04 実機検証）

- `POST /api/v1/direct_v2/threads/broadcast/text/` に実際にPOSTすると、JSONではなく
  **ログインページ**が返り、セッションが切れる挙動が出ました（認証は正常。GETは `status:"ok"`）
- 人が手で送ったDMを `chrome.webRequest` でネットワーク層から観測したところ、
  このエンドポイントへのPOSTは **65件のトレース中0件**。実際の送信は
  `POST /api/graphql`（Meta Comet/Relay の永続化クエリ。`doc_id` / `fb_dtsg` / `lsd` /
  `__dyn` `__csr` `__rev` などクライアントのビルドごとに変わる値を伴う）
- これを再現するのはIGのリリースごとに壊れる不安定な代物になり、
  モバイル私設APIとUA偽装は§5-3・§10 で禁止。よって**半自動が上限**です

### 安全装置（どれか1つでも欠けたら全自動を使わない）

- 送信間隔 45〜90秒・1分あたり1通・**1日30通**。popup からもパネルからも**緩められません**
- 日次上限は拡張側でも二重に数えます（ダッシュボードのカウントを信用しません）
- 「停止（キルスイッチ）」でループ先頭から即停止します
- `challenge` / `checkpoint` / `spam` / `feedback_required` を**1件でも踏んだらその回は全停止**します
- 実送信の関数は無効化済み（`content_ig.js` の `sendDirect` は常に unsupported を返す）。
  復活させるときは、**実際の送信リクエストを観測した記録を根拠として必ず添えること**

### 検証

```bash
node --test tests/dmcompose.test.js   # 文面・ガード・キュー・ステータス連動
node tools/e2e_dm_dryrun.mjs          # 送付ループ（stub。実送信 0件）
node tools/e2e_dm_panel.mjs           # パネル（要 python3 -m http.server 8765）
```
